# CI repair checkpoint — 2026-09-24

The preceding `2c71e28` edit accidentally truncated `apps/engine/src/signal.rs` by 631 lines while attempting a one-line Rust reborrow repair. The complete production signal engine was restored from the immediately preceding validated tree in `ce64dbd6` before any further feature work. This checkpoint exists to make the restoration auditable and to trigger the normal push CI workflow.

No performance, win-rate, or profitability claim is associated with this repair. Real-money autonomous execution remains disabled.
